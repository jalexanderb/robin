# Robin API — Python dependencies

# API framework — pinned to versions known to work with UploadFile Optional
fastapi==0.103.2
uvicorn[standard]==0.23.2
python-multipart==0.0.6

# Rate limiting
slowapi==0.1.9

# Database
psycopg2-binary==2.9.9

# HTTP client
httpx==0.25.0

# PDF rendering
reportlab==4.2.2

# PDF text extraction
pypdf==4.3.1

# HTML parsing
beautifulsoup4==4.12.3

# Object storage (S3-compatible) backend for blobs — only used when
# STORAGE_BACKEND=s3; imported lazily so the local/dev path doesn't need it.
boto3==1.34.131

# Test-only: mock S3 for the storage tests (`pip install moto` in CI/local).
# moto==5.0.*

# Payments — Stripe SDK; imported lazily, only used when STRIPE_SECRET_KEY is set.
stripe==9.12.0
