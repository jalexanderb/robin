{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "if [ -d portal ]; then cd portal && npm install; else npm install; fi",
  "buildCommand": "if [ -d portal ]; then cd portal && npm run build && rm -rf ../dist && cp -r dist ../dist; else npm run build; fi",
  "outputDirectory": "dist"
}
